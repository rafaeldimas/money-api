const router = require('express').Router()

router.use('/login', async (req, res) => {
  //
})

router.use('/register', async (req, res) => {

})

module.exports = {
  path: '/auth',
  router
}
